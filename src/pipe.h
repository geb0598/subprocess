#ifndef PIPE_H
#define PIPE_H

#include "readable.h"
#include "writable.h"

namespace subprocess {

class Pipe {
public:
    // TODO: is_readable (pipe[0] is opened)
private:

};

class PipeIn {
public:
private:

}

}

#endif